# media/

Ativos visuais do projeto (não código).

- `banner-mapa-mental.png` — imagem de capa clicável usada no README principal, linkando para `mapa-mental-vitalitas.pdf`.
- `mapa-mental-vitalitas.pdf` — mapa mental do projeto (visão geral de domínio/arquitetura).
- `diagramas/` — diagramas individuais (arquitetura, DER/MER, fluxos), um arquivo por diagrama.

Nenhum desses arquivos existe neste commit ainda — a pasta está preparada para receber os arquivos reais. Ao adicionar o banner e o PDF, mantenha exatamente esses nomes (ou atualize os links correspondentes em `../README.md`).
